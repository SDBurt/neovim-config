-- local.lua
-- Summary: Contains local or user-specific configurations, overrides, or settings to extend the base NeoVim setup.
-- This script allows for personalization of key mappings, editor settings, or plugin configurations,
-- making it easy to tailor the development environment to individual needs.

-- Example of setting a local key mapping
-- vim.api.nvim_set_keymap('n', '<leader>e', ':NvimTreeToggle<CR>', { noremap = true, silent = true })

local local_plugins = {
}
    

return local_plugins
